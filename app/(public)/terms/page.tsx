import { PublicPlaceholderPage } from "@/components/page-scaffold";
import { publicPages } from "@/lib/page-content";

export default function TermsPage() {
  return <PublicPlaceholderPage page={publicPages.terms} />;
}
